name: ai_calculator_flutter
description: A Flutter app scaffold for an AI-powered calculator with history, biometric login, and dark/light theme.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  local_auth: ^2.1.6
  provider: ^6.0.5
  shared_preferences: ^2.2.2
  # Add other dependencies for AI integration in future

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
